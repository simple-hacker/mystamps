name: CD
 
on:
  push:
    branches: [ master ]
 
jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
    - name: Deploy to production
      uses: appleboy/ssh-action@master
      with:
        username: michael
        host: mperks.co.uk
        key: ${{ secrets.SERVER_SSH_KEY }}
        script: 'cd /var/www/ && ./server_deploy.sh mystamps'
